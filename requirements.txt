# toknife 5.1.1 — 依賴說明
# ============================================
# 核心功能（零第三方依賴，僅需 Python 3.7+）
# 以下均為可選依賴，不安裝也能運行（會自動降級）

# 可選：精確 Token 計數（基準測試 / 複現 / 驗證腳本用）
# 不安裝時自動用 len(text)//3 粗略估算
tiktoken>=0.5.0

# 可選（示例用，重依賴）：語義快取示例
# 不安裝時自動降級到 jieba 或 2-gram 字符匹配
# sentence-transformers>=2.0
# jieba>=0.42
# numpy>=1.20
